# 火影战记 NarutoSenki — 安卓移植执行计划

> 创建:2026-06-22(Mac Mini,分支 `feat/revive-pain-orochimaru`)
> 最后更新:2026-07-30
> 本文是安卓移植的**执行蓝图**,与 `项目现状.md` 配套。结论性事实均经实际勘察(读源码、查 .mk、验 gitignore),推测处明确标注。
> 决策基线(已与用户确认):**路线 A(老 NDK + armeabi-v7a,先跑起来)+ 砍掉网络模块(不依赖 curl)+ 重新下载 2.2.6 框架补回预编译库**。
> **当前状态:阶段 0-5 已完成,APK 已发布,真机完整对局验证通过。此前战斗/奥义/结算崩溃的根因已确认并修复；当前主要平台限制为仅 `armeabi-v7a`。**

---

## 0. 一句话

游戏代码是跨平台的、现成的;移植的真正工作是**补回被 gitignore 排除的安卓预编译库 + 装老工具链 + 新建 `proj.android` 脚手架**。代码改动很少,环境与依赖才是大头。

---

## 1. 现状与障碍(已勘察)

| 项 | 状态 |
|---|---|
| 游戏代码 `Classes/` | ✅ 27 个 `.cpp` + `MyUtils/sqlite3.c`(C),平台无关,约 4.67 万行 |
| 框架各模块 `Android.mk` | ✅ 全在(cocos2dx / CocosDenshion / extensions / Box2D / chipmunk) |
| 参考模板 | ✅ `samples/Cpp/HelloCpp/proj.android`(JNI 入口 / Android.mk / Manifest) |
| `external/libwebsockets/android/*.a` | ✅ 已被 git 跟踪、本机已有(砍网络后用不到) |
| **`cocos2dx/platform/third_party/android/`** | ❌ **缺失(38M)**,被 `.gitignore:122` 故意排除 |
| **本机安卓工具链** | ❌ 一无所有:无 SDK/NDK,`java` 是报错桩,仅有 homebrew `adb` |
| `proj.android`(游戏侧) | ❌ 不存在,需从零建 |

**两个真正的拦路虎**:① 缺预编译图片库(png/jpeg/tiff/webp,引擎核心,砍不掉);② 工具链需从零安装(含**老 NDK**,2.2.6 的 `Application.mk` 写死 `APP_STL := gnustl_static`,现代 NDK r18+ 已删 gnustl 编不过)。

> curl 不在拦路虎里:走路线 A 砍掉 NetworkLayer/网络模块即可绕过(NetworkLayer 依赖的 HTTP 后端本就不存在,且与 LAN 目标无关)。

---

## 2. 前置条件(需用户动手,二者缺一不可)

### 2.1 补回安卓预编译库(38M)— 重新下载 2.2.6
- 下载 cocos2d-x 2.2.6 完整框架(官方下载源已死,用 **Internet Archive 镜像**:`https://archive.org/details/cocos2dx226`)。
- 从解压包里取出整个目录 `cocos2dx/platform/third_party/android/`,放到本仓库**同名路径**下。
- 校验:`ls cocos2dx/platform/third_party/android/prebuilt/` 应含 `libpng / libjpeg / libtiff / libwebp`(以及 `libcurl`,我们不用)。
- 这 38M **暂不纳入 git**(沿用原策略,本地放置即可);若日后要让安卓构建可复现,再在 `.gitignore` 局部放行 `!/cocos2dx/platform/third_party/android/`。

### 2.2 安卓工具链(Mac)
| 组件 | 版本/来源 | 备注 |
|---|---|---|
| JDK | 17(`brew install openjdk@17`) | 现机 `java` 是桩,必装 |
| Android SDK + 命令行工具 | `brew install --cask android-commandlinetools` | 装 `platform-tools`(已有 adb)、`platforms;android-XX`、`build-tools` |
| **NDK r10e** | `https://dl.google.com/android/repository/android-ndk-r10e-darwin-x86_64.zip` | 2.2.6 的标准搭档;x86_64 二进制,**Apple Silicon 需 Rosetta 2**(`softwareupdate --install-rosetta`) |
| (备选 NDK)r17c | 最后一个含 gnustl 的版本 | 若 r10e 在新版 macOS/Apple Silicon 上工具异常,改用 r17c(clang + gnustl_static 仍可编) |

> 环境变量:`export ANDROID_HOME=...`、`export NDK_ROOT=.../android-ndk-r10e`。

---

## 3. 构建策略:混合式(推荐)

cocos2d-x 2.2.6 原生的安卓构建是 **Eclipse + Ant**(`build.xml`/`.classpath`/`.project`),这套 2026 年已死(JDK17 跑不动老 ant、Eclipse ADT 绝迹)。两条打包路:

- **❌ 纯原版(Ant)**:复活 Eclipse/Ant 工具链,劝退。
- **✅ 混合式(推荐)**:
  - **Native 用老 NDK 编**:`ndk-build` + 仓库现成 `.mk` 文件 → 产出 `libnarutosenki.so`(armeabi-v7a)。
  - **打包用现代 Gradle**:建一个**精简 Gradle app 模块**,把上一步的 `.so` 放进 `jniLibs/armeabi-v7a/`、把 `cocos2dx/platform/android/java` 的 Java 源(`Cocos2dxActivity` 等)纳入、把 `Resources/` 拷进 `assets/`。用现代 Gradle/AGP 打 APK,`minSdk` 设低(如 19)、`compileSdk` 用新。
  - 好处:native 用老 NDK 保证 2.2.6 能编,打包用现代链保证能在 2026 的机器上出 APK、能 `adb install`。

---

## 4. 详细步骤(分阶段,每阶段有验证门)

### 阶段 0 — 环境就绪 ✅ 已完成
- JDK 17 / Android SDK / NDK r10e 安装完毕
- `third_party/android/prebuilt/` 四个图片库已补回
- 验证门通过: `ndk-build --version` / `adb devices` / 图片库在位

### 阶段 1 — 先编通框架 + HelloCpp(验证工具链) ✅ 已完成
- HelloCpp native `.so` 编译通过
- 验证门通过:老 NDK + 框架 `.mk` + 预编译库链路通了

### 阶段 2 — 建游戏 proj.android 脚手架 ✅ 已完成
- `jni/Android.mk` — 27 个 Classes .cpp + sqlite3.c + JNI 入口
- `jni/Application.mk` — `gnustl_static` + `armeabi-v7a`
- `AndroidManifest.xml` — 包名 `dev.leaderone.narutosenki`,横屏全屏
- Gradle 模块 — `build.gradle` / `settings.gradle` / wrapper 8.7 + AGP 8.5.2
- `build_native.sh` — 封装 `arch -x86_64 ndk-build` + `NDK_MODULE_PATH`
- 验证门通过:`./build_native.sh` 产出 `libcocos2dcpp.so`

### 阶段 3 — 砍网络模块(实际未执行,改为保留)
- 最初计划砍掉 NetworkLayer/curl,后来发现补回整个 `third_party/android/` 后 curl 已可用,且 NetworkLayer 的 HTTP 后端本就不存在(不影响运行),决定**保留 NetworkLayer 完整编译**。
- `extensions` 的 `Android.mk` 强拉 `cocos_curl_static`,已验证链接通过。
- **验证门**:`ndk-build` 编游戏 native 通过,0 错误。

### 阶段 4 — 复核安卓条件编译分支 ✅ 已完成
- `LoadLayer.h / GameOver.h / GameLayer.h` 的 `CC_PLATFORM_ANDROID` 分支已复核,无冲突。
- `setKeypadEnabled(true)` 安卓守卫在安卓上正常启用。
- `StartMenu::keyBackClicked` 的 JNI 对话框改为使用 `dev.leaderone.narutosenki` 包名下的 `AppActivity.openUrl()`(NEWS 按钮)和 `CCDirector::end()`(EXIT 按钮,不依赖已删除的 DialogUtils)。
- "三页选人 / 默认解锁 / 佩恩大蛇丸复活"在安卓上生效。
- **验证门**:真机运行正常。

### 阶段 5 — 打 APK 并真机运行 ✅ 已完成
- Debug APK + Release APK(正式签名)均已编译通过。
- 一加 13(Android 15,arm64)上完整运行:菜单→选角→对战→佩恩觉醒长门,并完成完整对局。
- **验证门(里程碑)通过**:游戏在安卓机上跑到主菜单并能进一局。
- **崩溃修复**:根因不是 `Hero::create()` 的随机生命周期问题，而是 `GameLayer::checkTower()` 的旧版防盗版校验：包名与原版不同时会清空 `_CharacterArray`，在防御塔销毁后触发战斗、奥义和结算的连锁空指针。已移除该破坏性分支，并补充必要的空指针保护。

---

## 5. 需要新建/修改的文件清单(阶段 2–3)

**新建**(全在 `projects/NarutoSenki/proj.android/`,纯新文件 UTF-8,不涉及 GBK 源):
- `jni/Android.mk`、`jni/Application.mk`、`jni/hellocpp/main.cpp`
- `AndroidManifest.xml`、`build.gradle`、`settings.gradle`、`gradle.properties`、`res/`(图标/strings)
- `src/.../<AppActivity>.java`(继承 `Cocos2dxActivity`)

**可能微调的现有源**(若需,**用字节安全 perl,勿用 Edit**,见 win32 gotchas):
- 仅在阶段 3/4 发现冲突时才动;目标是零改动或极小改动。

---

## 6. 已知坑与风险

1. **Apple Silicon × 老 NDK**:r10e 是 x86_64 二进制,需 Rosetta 2;个别报告其 `ndk-build` 在新 macOS 上有 make/awk 摩擦。备案:改用 r17c,或在 Win11/Linux 上编 native。
2. **gnustl 限制**:`gnustl_static` 对 C++11 支持有限;若游戏代码用到较新特性可能报错(win32 用 MSVC 编过不代表 gnustl 过)。届时逐个补。
3. **extensions 强依赖 curl**:其 `Android.mk` 默认 `+= cocos_curl_static`;砍网络需改这条或做变体,是阶段 3 的核心风险点。
4. **arm64-v8a 缺位**:路线 A 只出 armeabi-v7a(32 位)。绝大多数安卓机兼容 32 位可运行;极少数纯 64 位新机不行。要 64 位 = 升级到路线 B(另议)。
5. **资源大小写敏感**:安卓/Linux 文件系统大小写敏感,win32 不敏感;`Resources/` 里若有大小写不一致的引用,安卓上会 `Get data from file failed`(参考 win32 缺 XML 崩溃同类问题)。
6. **APK 体积**:含 52M 资源,APK 约 55M+,正常。

---

## 7. 与最终目标(LAN 联机)的衔接

安卓能跑起来后,才进入**真正的初心:无服务器局域网联机**。届时网络层另起方案(UDP 广播发现 + socket 同步),与现在砍掉的 HTTP NetworkLayer 无关。安卓的好处:同一 WiFi 下多台手机联机正是 LAN 对战最自然的形态。

---

## 8. 修订记录

- 2026-06-22:初版。决策基线=路线 A + 砍 curl + 重新下载补库;勘察完成,待用户完成阶段 0 前置条件后进入阶段 1。
- 2026-06-24:更新阶段 0-5 状态为已完成。实际未砍 curl(补回 third_party 后 curl 可用,NetworkLayer 保留完整编译)。工具链版本修正(temurin→openjdk)。补充遗留问题(野指针崩溃)。
- 2026-07-30:更新真机稳定性结论。确认并移除 `checkTower()` 的包名防盗版陷阱；完整对局验证通过，`armeabi-v7a` 仍是唯一 ABI。
