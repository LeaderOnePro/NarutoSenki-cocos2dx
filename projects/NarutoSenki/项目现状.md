# 火影战记 NarutoSenki — 项目现状地图

> 最后更新:2026-06-16
> 本文记录项目的真实完整度、构建/运行方式、已知问题与各平台移植缺口,作为后续开发的基准参考。
> 结论性事实均经过实际验证(编译、运行、读源码),非推测。推测处会明确标注。

---

## 0. 一句话概括

**这是一个完整、可运行的 cocos2d-x 2.2.6 游戏,但只带了 Windows 一个平台的打包工程。**
游戏本体(代码 + 资源)是完整的;Android / iOS / macOS / Linux 的工程文件都没有。
它是一份**开发者自用的工作副本快照**(停在某次迭代中间),不是对外打磨过的发布版导出——这解释了菜单里的若干"测试态"行为。

---

## 1. 项目完整度地图

### ① 游戏本体 —— 完整 ✅

| 部分 | 位置 | 说明 |
|---|---|---|
| 游戏逻辑 | `Classes/`(28 个 .cpp) | 平台无关 C++,约 46,700 行 |
| 资源 | `Resources/` | 音频/图集/地图/字体/特效,全套 |

战斗系统、50+ 角色专属 AI、技能/奥义/装备、HUD、排行榜、网络层——**功能齐全,能编译能运行**。

### ② 平台打包工程 —— 只有 Windows 一个

| 平台 | 工程目录 | 状态 |
|---|---|---|
| Windows | `proj.win32` | ✅ **有**,已迁移到现代 VS,能编能跑 |
| Android | `proj.android` | ❌ 无(需从零创建) |
| iOS | `proj.ios` | ❌ 无 |
| macOS | `proj.mac` | ❌ 无 |
| Linux | `proj.linux` | ❌ 无 |

> 注:连 `proj.win32` 都不是原版。原版基于 VS2010,当前这套是迁移后的版本。
> **目前能把本游戏编译出来的,可能仅此一套 Windows 工程。**

---

## 2. 源码结构速览

类层次(实体系统,基类 `ActionManager`):

```
ActionManager(8177 行,核心实体/动作)
├── HeroElement → Hero        英雄(可操控/AI,50+ 角色)
├── Flog                      召唤物(疑似 Frog,蛤蟆)
├── Tower                     防御塔
├── Monster                   小兵/怪物
└── Bullet                    弹道
```

塔 + 小兵 + 阵营(group)+ 带技能奥义的英雄 → 偏 **推塔竞技(MOBA)** 结构。

主要文件(按规模):

| 文件 | 行数 | 职责 |
|---|---|---|
| `Characters.cpp` | 8982 | 各角色定义与专属 AI 行为 |
| `ActionManager.cpp` | 8177 | 实体/动作核心,所有实体基类 |
| `StartMenu.cpp` | 3871 | 主菜单 + 模式路由(**测试态改动集中地**) |
| `Element.cpp` | 1994 | 实体类型(Hero/Flog/Tower/Monster/Bullet) |
| `RankingLayer.cpp` | 1898 | 在线排行榜 |
| `HudLayer.cpp` | 1440 | 战斗 HUD(摇杆 + 技能键 + 血条) |
| `GameLayer.cpp` | 1332 | 战斗主逻辑 |
| `NetworkLayer.cpp` | 1070 | 网络/联机(依赖后端) |
| `SelectLayer.cpp` | 438 | 选人界面(**完整但被架空**,见 §4) |

游戏流程:
```
AppDelegate → GameScene(logo/intro) → StartMenu(主菜单)
   ├── Training / Network / Credits
   ├── SelectLayer(选人)→ SkillLayer / GearLayer → LoadLayer
   └── GameLayer + HudLayer → PauseLayer / GameOver / RankingLayer
```

---

## 3. Windows 构建与运行(已验证)

### 工具链真相(⚠️ 与项目内迁移文档不符,以此为准)

原有迁移文档假设的是旧版 VS 开发环境，但**项目已直接升级到现代 VS 构建环境**：

- **Visual Studio 版本 18(即 VS2026)Professional**
  路径:`E:\Program Files\Microsoft Visual Studio\18\Professional`
- MSVC 工具集：`14.50.35717`，其 PlatformToolset 名为 **`v145`**
- **默认工具集升级**: 全套 7 个 vcxproj 项目的 `PlatformToolset` 均已被修改升级为 **`v145`**。
- Windows SDK:`E:\Windows Kits\10\`(含 `10.0.26100.0`),`WindowsTargetPlatformVersion=10.0` 自动取最新,无需改
- JDK 17 在 `E:\Program Files\Java\jdk-17`(安卓用,见 §6)

### 编译命令 (直接使用 v145 编译)

由于项目配置文件已被升级为默认使用 `v145` 工具集，直接在 VS 中点击“生成解决方案”或使用以下命令即可编译：

```bash
MSBUILD="/e/Program Files/Microsoft Visual Studio/18/Professional/MSBuild/Current/Bin/MSBuild.exe"
cd projects/NarutoSenki/proj.win32
"$MSBUILD" NarutoSenki.sln -p:Configuration=Debug -p:Platform=Win32 -m -nologo -v:minimal
```

- 会级联编译全部 6 个项目:chipmunk → Box2D → CocosDenshion → libcocos2d → libExtensions → NarutoSenki
- 首次约 1 分钟,**0 错误**(约 30 个无害警告:无符号比较、未用变量、`/Gm` 弃用等)
- 产物全部输出到 `proj.win32\Debug.win32\`:`NarutoSenki.exe`(~3MB)+ 全部依赖 DLL(libcocos2d/libExtensions/libCocosDenshion/glew32/libcurl/sqlite3/websockets/pthreadVCE2/zlib1 等)

### 运行方式(有坑)

游戏按**当前工作目录(CWD)**加载资源,不是 exe 所在目录。
依据:`cocos2dx/platform/win32/CCFileUtilsWin32.cpp:39` 用的是 `GetCurrentDirectoryW`。

- ✅ 正确:把工作目录设为 `Resources` 文件夹再启动 exe
- ❌ 错误:直接双击 `Debug.win32\NarutoSenki.exe`(找不到资源)
- DLL 与 exe 同在 `Debug.win32`,Windows 从 exe 目录加载,**不受 CWD 影响**

**一键启动器**:已创建 `projects/NarutoSenki/启动游戏.bat`,双击即可(已自动设好工作目录)。
手动启动(PowerShell):
```powershell
Start-Process "...\proj.win32\Debug.win32\NarutoSenki.exe" -WorkingDirectory "...\proj.win32\Resources"
```

启动成功标志:窗口标题 `NarutoSenki - 960x640 - 1.00` (已缩放为 2x 窗口)。

### Debug vs Release

- `Release.win32` 目录编译：目前也已升级，可以成功编译出 Release 版本。将来发布给他人：编 Release (更快更小)。命令同上，把 `Configuration=Debug` 换成 `Release`
- Debug/Release 是**同一份源码、不同编译选项**(优化、`_DEBUG`/`NDEBUG`、调试/发布 CRT),**游戏行为完全相同**
- 游戏逻辑代码里**没有任何** `_DEBUG`/`NDEBUG` 条件分支(命中的全在第三方 `sqlite3.c` 等)

---

## 4. 已完成的“测试态”清理与体验优化 (已修复)

在最近的迭代中，我们对游戏代码中残留的“测试态”调试行为进行了彻底清理与重构，并针对 PC 端体验进行了大量优化。

### ① PC 菜单返回与退出键支持
- **已修复**：移除了 4 处开启键盘接收 (`setKeypadEnabled(true)`) 的安卓平台守卫，现在 PC 平台下按 `ESC` 或 `F1` 能够正常触发返回事件。
- **退出功能**：在 `StartMenu` 的主菜单中，现在按下 `ESC` 或点击新增的 PC 专属 **Exit (退出)** 按钮即可正常关闭游戏。

### ② 选人界面与离线“羁绊模式”全面恢复
- **已修复**：取消了主菜单 Training（训练）按钮跳转到 `SelectLayer` 的注释限制。
- **组队机制**：Training 按钮现已完美重定向至 `NetworkLayer`（支持多人组队选人与技能/装备选择），从而真正开启了多人离线大乱斗的“羁绊模式”。

### ③ 解除角色写死纲手限制
- **已修复**：去除了原版在训练模式中强制写死千手纲手（Tsunade）的代码分支。现在角色完全由玩家在选人界面中手动选取，队友与对手也均从全名单中随机产生。
- **级联恢复**：随着选人界面的恢复，配套的 **SkillLayer (选技能)** 与 **GearLayer (选装备)** 也已级联恢复，且强制硬核模式的限制已解除，游戏难度自动切回普通模式。

### ④ 窗口放大与画面优化
- **窗口放大**：将 Win32 窗口的分辨率从原始的 480x320 放大到 **2x (960x640)**，解决高分屏下窗口过小的问题。
- **NEWS 按钮功能**：点击主菜单的 NEWS 按钮现在会在 Win32 系统下自动调用默认浏览器打开火影战记的 GitHub 仓库。

### ⑤ 战斗 AI 自动启动
- **AI 角色开局即行动**：恢复了 GameLayer 中对于 `doAI()` 初始化的调用。修复了原版战斗开始时，Com(AI) 角色全部在原地站立不动的 Bug，现在战斗一开始 AI 就会立即参与攻防。

### ⑥ 精灵帧容错崩溃保护
- **引擎健壮性修复**：当核心渲染引擎在加载部分特定角色的资源图集时如果遇到“缺失精灵帧”的问题，原版 cocos2d-x 会直接崩溃。现已修改底层代码使其不再崩溃，而是返回一个空精灵（容错行为匹配安卓 Release 版本），大幅提升了 PC 端的稳定性。

---

## 4b. 测试态改动清单与修复状态 (截至 2026-06-16)

### A. 测试态改动 —— 已全部修复 ✅

| # | 现象 | 位置 | 修复方案 | 状态 |
|---|---|---|---|---|
| A1 | PC 返回键失效 | `StartMenu.cpp:482` 等 4 处 | 去除 `#if ANDROID` 安卓守卫，所有平台启用 | 已修复 ✅ |
| A1+ | 主菜单 ESC 无行为 | `StartMenu::keyBackClicked()` | 为 PC 端增加退出确认，ESC 或双击可直接退出游戏 | 已修复 ✅ |
| A2 | 选人界面被架空 | `StartMenu.cpp:3380` | 取消 `SelectLayer::create()` 跳转注释，Training 按钮成功接线 | 已修复 ✅ |
| A3 | 角色写死纲手 | `StartMenu.cpp:3427` | 去除写死 Tsunade 逻辑，完全根据 `SelectLayer` 传入的角色值进行加载 | 已修复 ✅ |
| A4 | AI 角色开局原地不动 | `GameLayer.cpp` | 启用 `doAI()`，开局自动激活所有 AI 决策逻辑 | 已修复 ✅ |

---

## 5. 后端依赖与联机现状

- `NetworkLayer`(1070 行)+ `Defines.h` 里 `SERVER "http://localhost/"` + `MemberID`/`GroupID`/排行榜
- 原设计依赖一个 **HTTP 后端服务器**(账号、排行、联机),该服务端**未随源码提供**(正常:客户端源码本就不含服务端)
- 还有 `Cheats` 全局变量、MD5 校验(`CMD5Checksum`,完整性/反作弊)
- **目标方向**:改造成**无服务器的局域网(LAN)联机**。选人界面(§4②)正是联机前选角色的入口,需先恢复。

---

## 6. 各平台移植缺口 & 补法

| 平台 | 缺什么 | 补法与难点 |
|---|---|---|
| **Android** | `proj.android` + NDK 兼容 | 用 `samples/Cpp/HelloCpp/proj.android` 作模板从零建(`jni/Android.mk` 列全 Classes、`AndroidManifest.xml`、继承 `Cocos2dxActivity` 的 Java 入口、`Resources`→`assets/`)。**最大坑**:框架 `Application.mk` 用 `APP_STL := gnustl_static`,而本机 NDK 是 r26/r27(gnustl 在 r18 已删、仅 clang、最低 API 21)→ 编不过。两条路:① 装回老版 **NDK r10e**(2.2.6 配套,改动最小);② 移植到现代 NDK(改 `c++_static` + 修一堆 libc++/clang 报错,工作量大)。打包还需自写 Gradle(老式 Eclipse+Ant 已废,JDK17 不兼容) |
| **iOS** | `proj.ios` | 需 macOS + Xcode 环境。建 `proj.ios`,`Classes`/`Resources` 复用 |
| **macOS** | `proj.mac` | 同上,需 macOS + Xcode |
| **Linux** | `proj.linux` + Makefile | 需 Linux 环境(框架根有 `make all` 体系可参考) |
| **Windows(Release)** | 仅未编译 | 一条命令(§3,改 `Configuration=Release`) |

> 通用规律:换平台 = 建该平台的 `proj.xxx` 脚手架(游戏 `Classes`/`Resources` 原样复用),**难点在工具链不在游戏代码**。
> 游戏代码里仅有少量平台特化:`StartMenu::keyBackClicked` 的 JNI 退出对话框、`setKeypadEnabled` 安卓守卫——这也佐证**作者当年主力平台是安卓**,Windows 工程偏开发调试用。

---

## 7. 目标与下一步

**核心目标**:给这款 2016 年停更的游戏加入**局域网联机对战**(无需服务器),用于个人/朋友间的怀旧重温。

建议推进顺序(均在 Windows 上做,迭代快):
1. ~~修返回键(§4①)~~ —— 已完成 ✅
2. ~~恢复选人界面 + 去掉纲手写死 + 激活战斗 AI(§4②③⑤)~~ —— 已完成 ✅
3. **调研并落地局域网联机**(§5) —— 评估改造 `NetworkLayer` 走 P2P/LAN，还是另起模块（例如集成 ENet 或 SDL_net 等轻量级网络库）。
4. **Android 等平台移植**(§6) —— 作为发布里程碑，在玩法与局域网联机跑通之后开始推进。
5. **解禁限制角色** —— 正在 `feat/revive-pain-orochimaru` 分支下进行佩恩/大蛇丸的解禁工作。

Android 不是"正常版"的捷径:它同样有测试态行为,只是硬件返回键能用。目前 Windows 端已是体验最完善的调试与游玩版本。

---

## 附:关键文件 file:line 速查

| 事项 | 位置 |
|---|---|
| 资源按 CWD 解析 | `cocos2dx/platform/win32/CCFileUtilsWin32.cpp:39` |
| ESC/F1→返回 映射 | `cocos2dx/platform/win32/CCEGLView.cpp:458-460` |
| 返回键安卓守卫(已修复) | `StartMenu.cpp:482`、`SelectLayer.cpp:214`、`NetworkLayer.cpp:413`、`CreditsLayer.cpp:82` |
| 选人跳转(已还原) | `StartMenu.cpp:3380-3383` |
| 写死纲手(已解除) | `StartMenu.cpp:3427` |
| 选人界面进场流程 | `SelectLayer.cpp:402-408` |
| 后端地址/版本号 | `Defines.h:30-31`(`SERVER` / `CURRENT_VERSION 12200`) |
| 一键启动器 | `projects/NarutoSenki/启动游戏.bat` |

---

## 版本控制(git)

- 整个 `cocos2d-x-2.2.6/` 已是一个 git 仓库(分支 `main`),首次提交记录"能编译能运行"的状态。
- **范围**:框架 + 游戏 + 迁移补丁全在一个 repo,`clone` 下来即可编译(无需另外下载 cocos2d-x 再打补丁)——这对将来供二创最友好。
- **`.gitignore` 策略**:忽略编译产物(`*.win32/`)、IDE 缓存(`.vs/`)、迁移前备份(`projects/NarutoSenki - 副本/`,留本地、不跟踪)、非 win32 平台的预编译依赖与脚本引擎(emscripten / spidermonkey / luajit、third_party 下非 win32 的平台子目录);**保留** win32 构建必需的预编译依赖(`third_party/win32`、`libwebsockets`、`sqlite3`、`CocosDenshion/third_party`)。
- 原版 `LeaderOnePro/NarutoSenki`(GitHub)保持为"纯净原版存档";本仓库定位为 `NarutoSenki-cocos2dx` 现代化重构项目。
- 注: 框架与游戏全部 7 个项目的 vcxproj 配置文件已统一升级为 `v145` 工具集（VS2026），支持 VS2026 直接打开编译。不再需要 MSBuild 命令行传递 PlatformToolset 参数覆盖。

## 修订记录

- 2026-06-13:首次整理。完成 Windows(VS2026/v145)编译并成功运行到主菜单;查清完整度地图、测试态行为根因、各平台缺口。
- 2026-06-13:整个 cocos2d-x-2.2.6 树纳入 git 版本控制(`main` 分支,首次提交 `73efb1a`),配置 monorepo 化的 `.gitignore`。
- 2026-06-13:完成"测试态改动"全量排查,新增「4b. 测试态改动完整清单」一节(A 要修 / B 联机日后重做 / C 别动 / D 次要),供一次性协调修复时照单逐项执行。
- 2026-06-16:完成首批测试态代码清理（返回键、退出对话框、选人界面、大乱斗组队、纲手解锁、AI 战斗激活等），升级全部 vcxproj 项目文件至 v145 (VS2026) 默认工具集，更新相关迁移指南，重构 README.md 并补充安装部署快速上手指南。
