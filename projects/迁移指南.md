# 火影战记项目迁移指南

## 当前问题分析
- 项目使用VS2010工具集(v100)但文件格式为VS2017(ToolsVersion=15.0)
- Windows SDK版本冲突(v7.1A vs Win11现代SDK)
- 编译器C++标准差异导致的语法错误

## 迁移步骤

### 步骤1：项目配置现代化

修改 `NarutoSenki.vcxproj` 中的关键配置：

```xml
<!-- 替换工具集配置 -->
<PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Release|Win32'" Label="Configuration">
  <ConfigurationType>Application</ConfigurationType>
  <CharacterSet>Unicode</CharacterSet>
  <WholeProgramOptimization>true</WholeProgramOptimization>
  <PlatformToolset>v145</PlatformToolset>  <!-- VS2026工具集 -->
  <WindowsTargetPlatformVersion>10.0</WindowsTargetPlatformVersion>
</PropertyGroup>

<PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Debug|Win32'" Label="Configuration">
  <ConfigurationType>Application</ConfigurationType>
  <CharacterSet>Unicode</CharacterSet>
  <PlatformToolset>v145</PlatformToolset>  <!-- VS2026工具集 -->
  <WindowsTargetPlatformVersion>10.0</WindowsTargetPlatformVersion>
</PropertyGroup>
```

### 步骤2：库路径现代化

```xml
<!-- 移除旧SDK路径 -->
<PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Debug|Win32'">
  <!-- <LibraryPath>$(MSBuildProgramFiles32)\Microsoft SDKs\Windows\v7.1A\lib;$(LibraryPath)</LibraryPath> -->
  <EmbedManifest>false</EmbedManifest>
</PropertyGroup>
```

### 步骤3：编译器标志更新

```xml
<ClCompile>
  <Optimization>Disabled</Optimization>
  <AdditionalIncludeDirectories>$(ProjectDir)..\..\..\cocos2dx;$(ProjectDir)..\..\..\cocos2dx\include;$(ProjectDir)..\..\..\cocos2dx\kazmath\include;$(ProjectDir)..\..\..\cocos2dx\platform\win32;$(ProjectDir)..\..\..\cocos2dx\platform\third_party\win32;$(ProjectDir)..\..\..\cocos2dx\platform\third_party\win32\OGLES;$(ProjectDir)..\..\..\external;$(ProjectDir)..\..\..\external\chipmunk\include\chipmunk;$(ProjectDir)..\..\..\CocosDenshion\include;$(ProjectDir)..\..\..\extensions;..\Classes;..;%(AdditionalIncludeDirectories)</AdditionalIncludeDirectories>
  <PreprocessorDefinitions>WIN32;_DEBUG;_WINDOWS;_USE_MATH_DEFINES;GL_GLEXT_PROTOTYPES;CC_ENABLE_CHIPMUNK_INTEGRATION=1;COCOS2D_DEBUG=1;_CRT_SECURE_NO_WARNINGS;_SCL_SECURE_NO_WARNINGS;HAVE_STRUCT_TIMESPEC;%(PreprocessorDefinitions)</PreprocessorDefinitions>
  <!-- 添加现代C++兼容性标志 -->
  <LanguageStandard>stdcpp14</LanguageStandard>
  <ConformanceMode>false</ConformanceMode>
  <RuntimeLibrary>MultiThreadedDebugDLL</RuntimeLibrary>
  <PrecompiledHeader></PrecompiledHeader>
  <WarningLevel>Level3</WarningLevel>
  <DebugInformationFormat>ProgramDatabase</DebugInformationFormat>  <!-- 替换EditAndContinue -->
  <DisableSpecificWarnings>4267;4251;4244;4996;%(DisableSpecificWarnings)</DisableSpecificWarnings>
</ClCompile>
```

### 步骤4：常见编译错误修复

1. **sprintf 安全警告**
```cpp
// 替换不安全的sprintf
sprintf_s(buffer, sizeof(buffer), "格式化字符串", args...);
```

2. **std命名空间问题**
```cpp
// 添加明确的std命名空间
using std::min;
using std::max;
```

3. **时间函数兼容**
```cpp
// 如果遇到timespec问题，添加：
#ifdef WIN32
#ifndef HAVE_STRUCT_TIMESPEC
struct timespec {
    long tv_sec;
    long tv_nsec;
};
#endif
#endif
```

### 步骤5：第三方库更新

检查并更新以下库到 VS2026 兼容版本：
- glew32.lib
- libcurl
- pthread
- websockets

### 步骤6：测试验证

在 Visual Studio 2026 中直接双击打开项目：
`projects/NarutoSenki/proj.win32/NarutoSenki.sln`

手动编译步骤：
1. 选中解决方案，右键选择“生成解决方案”
2. 或者在 MSBuild 终端下直接执行编译命令（无需传递 `/p:PlatformToolset`，项目默认工具集已升级）
3. 编译成功后运行 `启动游戏.bat` 开始体验

### VS2026 路径配置

根据实际安装的 Visual Studio 版本（以 VS2026 路径为例）：
- 安装路径: `E:\Program Files\Microsoft Visual Studio\18\Professional\`
- MSBuild.exe: `E:\Program Files\Microsoft Visual Studio\18\Professional\MSBuild\Current\Bin\MSBuild.exe`
- devenv.exe: `E:\Program Files\Microsoft Visual Studio\18\Professional\Common7\IDE\devenv.exe`

## 备选方案

如果直接迁移遇到困难，可以考虑：

1. **容器化开发**：使用Docker配置VS2010环境
2. **虚拟机方案**：在VM中使用旧版开发环境
3. **框架升级**：迁移到cocos2d-x 3.x或4.x版本

## 迁移清单

- [ ] 备份原项目
- [ ] 修改项目文件配置  
- [ ] 更新编译器标志
- [ ] 解决编译错误
- [ ] 测试游戏功能
- [ ] 优化现代开发体验